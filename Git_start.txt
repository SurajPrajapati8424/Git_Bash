git status 	//to chechk whether it is git repo or not !

git init	//To inintalize 

git add --a 	//--a for ALL  insert into staged area

git commit -m   // -m for Massege. without m it opens EDITOR

git log		// log Display every commit & action on file

rm -rf .git 	// Remove All File --- NO TRACK OF FILE.

